//запросы при инициализации (
		(в конструкторе объявляются привязываемые методы)
	по готовности документа методом инит
		(в нём сначала привязка событий, затем методы запроса)
) и нажатии кнопок


//в конце запроса метод берёт прочитанный ответ и вставляет в html
